- Started on branch `codex/replay-csv-decision-context`; repo status was clean before branching. Next: inspect replay CSV tests/schema and write the failing decision-context export test.
- Added red test `replay CSV exposes pre-action hand state and newest pickup context` in `test/replay.test.js`; it expects pre-action replay snapshots, `pre_hand_card` CSV rows, pre-actor newest-card fields, and picked-up card identity fields. Next: run the replay test and confirm the expected failure.
- Ran `node --test test/replay.test.js`; expected red failure occurred in the new test because `discardEvent.preHands` is missing. Next: implement pre-action replay capture and CSV context columns.
- Updated `server.js` to capture `preHands`/`preTable`/`preKnowledge` on action replay events and export `pre_hand_card`, pre-board, pre-actor-newest, picked-up-card, and `is_newest_card` CSV fields. Next: rerun replay tests and fix any schema-doc gaps.
- Ran `node --test test/replay.test.js`; new decision-context test passed, remaining failure was the existing docs guard for undocumented `picked_up_card_id`. Next: update `docs/replay-csv.md`.
- Trimmed redundant CSV fields after user review: removed duplicated `picked_up_card_*` and `pre_actor_newest_card_*`; kept pre-action snapshots/rows, `is_newest_card`, and replacement color/rank beside existing `replacement_card_id`. Next: document the leaner schema.
- Updated `docs/replay-csv.md` for `pre_hand_card`, `pre_*` board fields, `is_newest_card`, and replacement color/rank. Next: rerun replay tests.
- Removed pre-snapshots from the synthetic `end-game` event so only real player actions produce pre-action context. Next: run syntax and relevant node tests.
- Hid internal `preHands`/`preTable`/`preKnowledge` from `/api/replay` JSON via `publicReplayEvent`; CSV export still uses the internal snapshots. Updated the test to guard that replay JSON remains playback-focused. Next: rerun syntax and test commands.
- Ran `node --check server.js`, `node --check public/app.js`, and `node --test test/replay.test.js test/turns.test.js test/replay-fuzz.test.js`; all passed after hiding pre snapshots from replay JSON. Next: run the full node test suite as final guard.
- Ran `node --test test/*.test.js`; full suite passed with 70 tests. Remaining gaps: none known; no deploy or PR was created.
- User requested trimming redundant replacement identity fields and pushing. Updated `test/replay.test.js` first to require `replacement_card_color` and `replacement_card_rank` to be absent from the CSV header. Next: run focused replay test for the expected red failure.
- Ran `node --test test/replay.test.js`; expected red failure occurred because `replacement_card_color` was still present in the header. Removed `replacement_card_color` and `replacement_card_rank` from `server.js` and updated `docs/replay-csv.md`. Next: rerun checks.
- Ran `node --check server.js`, `node --check public/app.js`, and `node --test test/replay.test.js test/turns.test.js test/replay-fuzz.test.js`; all passed after the replacement identity trim. Next: run the full suite, commit, and push.
- Ran `node --test test/*.test.js`; full suite passed with 70 tests after the replacement identity trim. Next: commit and push `codex/replay-csv-decision-context`.
